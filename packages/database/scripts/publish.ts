import { readFile } from 'fs/promises'; import { resolve } from 'path'; import { PrismaClient } from '@prisma/client'; async function main() { const content = await readFile(resolve(process.cwd(), '.env'), 'utf8').catch(() => ''); for (const line of content.split(/\r?\n/)) { const trimmed = line.trim(); if (!trimmed || trimmed.startsWith('#')) continue; const separator = trimmed.indexOf('='); if (separator < 1) continue; const key = trimmed.slice(0, separator); const value = trimmed.slice(separator + 1); process.env[key] ??= value; } const prisma = new PrismaClient(); await prisma.collection.update({ where: { slug: 'world-cup-2026' }, data: { status: 'PUBLISHED' } }); console.log('Status updated to PUBLISHED'); await prisma.$disconnect(); } main().catch(console.error);
